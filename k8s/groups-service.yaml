---
# ── groups-service Deployment ──────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: groups-service
  namespace: groupsapp
  labels:
    app: groups-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: groups-service
  template:
    metadata:
      labels:
        app: groups-service
    spec:
      containers:
        - name: groups-service
          image: groupsapp-groups-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50052
              name: grpc
          envFrom:
            - configMapRef:
                name: groupsapp-config
            - secretRef:
                name: groupsapp-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            exec:
              command:
                - node
                - -e
                - "require('@grpc/grpc-js').credentials.createInsecure(); process.exit(0)"
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - node
                - -e
                - "require('@grpc/grpc-js').credentials.createInsecure(); process.exit(0)"
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: groups-service
  namespace: groupsapp
spec:
  selector:
    app: groups-service
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
---
# ── HPA for groups-service ─────────────────────────────────────────────────────
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: groups-service-hpa
  namespace: groupsapp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: groups-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 70
